[package]
name = "limitless"
version = "0.1.0"
edition = "2024"

[dependencies]
cfg-if = "1.0.4"

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)'] }
